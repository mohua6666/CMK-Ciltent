<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MKPVPClient"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.AccountActivity"
            android:theme="@style/Theme.MKPVPClient" />

        <activity
            android:name=".ui.SkinActivity"
            android:theme="@style/Theme.MKPVPClient" />

        <activity
            android:name=".ui.VersionManagerActivity"
            android:theme="@style/Theme.MKPVPClient" />

        <activity
            android:name=".ui.ModManagerActivity"
            android:theme="@style/Theme.MKPVPClient" />

        <activity
            android:name=".ui.ResourcePackActivity"
            android:theme="@style/Theme.MKPVPClient" />

        <activity
            android:name=".ui.SaveManagerActivity"
            android:theme="@style/Theme.MKPVPClient" />

        <activity
            android:name=".ui.KeyMappingActivity"
            android:theme="@style/Theme.MKPVPClient" />

        <activity
            android:name=".ui.SettingsActivity"
            android:theme="@style/Theme.MKPVPClient" />

        <activity
            android:name=".ui.PVPClientActivity"
            android:theme="@style/Theme.MKPVPClient" />

        <activity
            android:name=".ui.DownloadActivity"
            android:theme="@style/Theme.MKPVPClient" />

    </application>

</manifest>
